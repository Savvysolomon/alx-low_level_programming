#include <stdio.h>
/**
*main - entry point
*return: Always reeturn 0 (Success)
*/
int main(void) /* main function */
{
char a;
for (a = 'a'; a <= 'z'; a++)
putchar(a);
for (a = 'A'; a <= 'Z'; a++)
putchar(a);
putchar (a);
putchar ('\n');
return (0);
}
